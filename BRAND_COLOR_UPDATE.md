# Spratt Brand Color Update

## Color Extracted from Logo Image

**New Spratt Blue**: `#1c4668` (RGB: 28, 70, 104)

This color was extracted directly from the Spratt logo image showing "PRA" text.

## Changes Made

✅ Updated `src/index.css` - Changed `--color-spratt-blue` from `#00407A` to `#1c4668`
✅ Updated all hover states to use opacity instead of hardcoded colors
✅ All components now use the exact Spratt brand color

## Color Usage

- **Primary Brand Color**: `#1c4668` (Spratt Blue)
- **Secondary Color**: `#666666` (Spratt Grey) - unchanged
- **Hover States**: Use opacity modifiers (`hover:opacity-90`, `hover:opacity-80`)

## Components Updated

All components using `text-spratt-blue`, `bg-spratt-blue`, `border-spratt-blue` now automatically use the new color `#1c4668`.
